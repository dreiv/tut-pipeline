{
  "extends": "./tsconfig.app.json",

  "include": ["src/**/*.{test,spec}.ts", "env.d.ts"],
  "exclude": [],

  "compilerOptions": {
    "composite": true,
    "lib": ["ESNext", "DOM", "DOM.Iterable"],
    "types": ["node", "vitest/browser", "vitest/globals"],

    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.vitest.tsbuildinfo"
  }
}
